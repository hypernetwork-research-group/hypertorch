name: Weekly CI

on:
  schedule:
  - cron: "0 0 * * 0"  # every week at 00:00 CET (6:00 PM EST)
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      with:
        persist-credentials: false

    - name: Install uv with Python ${{ matrix.python-version }}
      uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        uv init
        uv add hypertorch
        uv add lightning

    - name: Clone repository
      run: |
        git clone https://github.com/hypernetwork-research-group/hypertorch.git

        mv hypertorch/examples examples

        mv hypertorch/scripts/run_examples.sh .
        chmod +x run_examples.sh

        ./run_examples.sh 2>&1 | tee run_examples_out.txt

        if grep -q "^The following examples failed:" run_examples_out.txt; then
          echo "===== Example failures detected ====="
          sed -n '/^The following examples failed:/,$p' run_examples_out.txt
          exit 1
        fi
